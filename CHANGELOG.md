# 更新日志

本文件汇总各版本的对外变更；每个版本的完整说明见对应的
[Release](https://github.com/maozhongmao/Spaceflight-simulator-Multiplayer/releases)。

---

## V1.2.4

**性能**

- 服务端整体重写为 C++ 实现：不再依赖 .NET 运行时，产物为单个可执行文件，下载即用。
- 单线程事件循环，无 GC 停顿；实测只占一个核心的个位数百分比，慢客户端不会拖慢其他玩家。
- 与上一代 C# 服务端在同负载下对比：CPU 开销相差 **5 ~ 8 倍**，常驻内存相差 **2 ~ 4 倍**
  （512 个碰撞箱：2.4 MB / 1.12% CPU 对比 10.17 MB / 9.2%；8192 个碰撞箱：18.71 MB / 5.7% 对比 37.79 MB / 17.7%）。
- 修掉「建→删→重建」风暴：无主火箭的创建宽限由 0 秒改为 60 秒，实测该循环归零（此前一轮会产生 61 次创建 / 47 次销毁包）。
- 修掉「无人操控的火箭按固定方向一直飞」：这类火箭不再滞留在 Kinematic 外推状态，交由本地引擎推进。

**登录与连接**

- 新增 SRV 免端口连接：连接地址只填域名即可，客户端自动查询按域名查询 SRV 记录取得端口。
- 新增服务端与客户端版本一致性检查：登录页显示双方版本与 Ping；主版本不一致时禁用 Join 并提示。
- 新增断线自动重连：带令牌退避重连，成功后原地回到世界，不退回主菜单。

**远端火箭同步**

- 同步模型重写：改为「每个权威包整份状态写回 + 包间隔内引擎外推 + 自适应渲染延迟 + 断流后交回本地物理」。
- P2P 从玩家火箭状态链路摘除，严格服务器单源；P2P 默认关闭（`--p2p on` 可开）。
- 发射台逻辑改回上游实现：占用时拦截发射，被无主火箭阻挡时提供一键清场。

**服务端与协议**

- 帧类型白名单补齐到 12（此前新版登录帧会被判非法并掐断会话）。
- 新增版本探测帧。
- KCP 会话仅在 IP 变化时重建，仅端口漂移不再重建会话。

**已知问题**

- 管道部件（Pipe / CustomPipe）导致的客户端崩溃尚未加防护。
- 发布包中保留诊断日志，便于问题复现。

---

## V1.1.4.5

- 客户端拆分为 **SFS 1.5** 与 **SFS 1.6** 两个包（文件名中的 1.5 / 1.6 即对应的游戏版本，装错会报 vtable 错误）。
- 修复若干联机稳定性问题。

## V1.1.4

- 新增玩家间直连（P2P）：服务端按玩家位置与网络端点撮合，配对后玩家之间直接传输火箭状态，中断时自动回退服务器中转。
- 新增权威分配与控制权协调：状态更新权威按玩家与火箭的关系分配，当前控制该火箭的玩家优先。
- 新增插值渲染策略与网络自适应参数：平滑量与数据包间隔成比例，避免高延迟链路下过度平滑导致操作迟滞。
- 新增太空垃圾自动清理：失去控制且部件数低于阈值的碎片自动移除，阈值可配置。
- 新增实验性功能访问与服务端插件（补丁）系统。
- 网络传输：UDP 绑定增加心跳重试；可靠业务包与高频状态包按类型分流。
