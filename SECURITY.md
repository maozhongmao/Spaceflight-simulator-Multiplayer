# 安全策略

## 支持的版本

只有最新的发布版本接收安全修复。

| 版本 | 支持 |
|---|---|
| 1.1.4 | ✅ |
| 更早版本 | ❌ 请升级到最新版 |

## 如何报告漏洞

**请不要通过公开 Issue、讨论区或 QQ 群报告安全漏洞。**

请私下联系：

- 邮箱：maozhongmao@qq.com
- 备用：yangchengtong@stch.de5.net

报告中请说明：受影响的版本、复现步骤、影响范围（例如：能让服务端崩溃 / 能绕过加入密码 / 能越权控制他人火箭）。

我们会在确认后评估并发布修复。在修复发布前，请不要公开漏洞细节。

## 不属于安全问题的情形

- 需要修改客户端或服务端文件才能触发的行为
- 需要服务端管理员权限才能执行的操作
- 纯配置问题（例如没有设置 `password`，导致服务器对任何人开放）
- 第三方组件自身的漏洞（请向对应上游项目报告；本项目使用的第三方组件见 `Server/THIRD-PARTY-NOTICES.md`）

## 安全相关实现说明

- 服务端可在 `server.yml` 的 `password` 字段，或环境变量 `SFS_SERVER_PASSWORD` 中设置加入密码。
- UDP 通道使用登录后下发的一次性会话令牌进行绑定校验。
- 请不要把填有密码的 `server.yml` 上传到公开仓库。
