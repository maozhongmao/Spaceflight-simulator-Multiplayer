# 更新计划

本文件记录**接下来要做的事**，以及**已经完成并已随版本发布**的部分。

> 本文件只描述状态，不承诺具体日期。已发布版本的行为请以 [Releases](https://github.com/maozhongmao/Spaceflight-simulator-Multiplayer/releases) 与 [项目维基](https://github.com/maozhongmao/Spaceflight-simulator-Multiplayer/wiki) 为准。

## 一、已随 V1.2.4 发布

| 项 | 说明 |
| --- | --- |
| **KCP 传输重构** | 传输层从"TCP + UDP 双通道"统一为 **KCP（基于 UDP 的可靠传输）**：可靠送达与低延迟合并到一条通道上，消除双通道分流与切换带来的边界问题（首次连接偶发失败、状态与业务包不同步等）；实测延迟明显下降 |
| **性能优化** | 服务端使用 **C++ 重写**，不再依赖 .NET 运行时，产物为单个可执行文件、下载即用；单线程事件循环无 GC 停顿，慢客户端不再拖慢其他玩家。客户端侧：远方火箭爆炸特效按帧限流、发射/加载界面兜底关闭、已存在的火箭不再被反复重建、行星地形查询异常保护。另有“建→删→重建”风暴与无人操控火箭按固定方向飞行的修复 |
| **C++ 低占用服务端** | 面向低内存设备：几十 MB 内存可长期运行，与现有客户端使用同一套线格式；发布页提供五个平台的可执行文件 |
| **断线自动重连** | 短时间掉线后带令牌退避自动重连，成功后原地回到世界，不再退回主菜单 |
| **连接与登录改进** | 支持只填域名连接（自动查询 SRV 记录取得端口）；登录页常驻显示双方版本与 Ping，主版本不一致时禁用进入；新增版本探测帧 |
| **源码与文档** | 客户端与服务端源码已上传；更新日志（`CHANGELOG.md`）与项目维基同步更新 |

## 二、接下来要做

| 项 | 说明 |
| --- | --- |
| **服务端内存压缩** | 把火箭的部件数据以压缩形式常驻，削掉加载期内存峰值，让更大规模的碰撞箱数量在几十 MB 内存的设备上也能稳定运行 |
| **控制权变更收尾** | 火箭分离 / 对接后控制权自动迁移到正确玩家，减少“无主火箭”与切权瞬间的位姿跳变 |
| **加载期竞态修复** | 世界加载阶段的偶发异常（重复创建、状态错位）继续收敛，目标是长时间游玩更稳 |
| **网络自适应调参** | 按实测链路质量调整插值上限与状态发送间隔，兼顾高延迟下的操作手感与画面平滑 |
| **多人压力测试** | 提高同时在线人数，观察服务端 CPU / 内存 / 带宽，给出各档机器的推荐人数配置 |
| **低配设备部署文档** | 面向开发板与小机器的部署建议：单实例约束、存档备份、内存与虚拟内存调优、开机自启；并说明**使用内网穿透时隧道传输协议要选 TCP（常规），不要选 QUIC / KCP，且不要启用 Proxy Protocol** |
| **跨版本兼容** | 尽量保持旧版协议兼容；只有线格式真正不兼容时才提升协议版本，并用旧版客户端与服务端做交叉验证 |

## 三、平台支持现状

- **客户端**：SFS 1.6 与 SFS 1.5 各一个包（文件名中的 `1.6` / `1.5` 即对应游戏版本）；
- **服务端**：Windows（x64 / ARM64）、Linux（x86-64 / ARM64 / ARMv7 32 位），五个平台功能与协议完全一致，可以互相联机；
- 低内存设备（ARMv7、几十 MB 内存）的运行数据与部署建议见项目维基。

## 四、参与与反馈

- 使用问题与联机求助：QQ 群 **679991439**；
- 安全问题（漏洞、可利用缺陷）：按 [`SECURITY.md`](SECURITY.md) 通过联系邮箱报告，请勿在公开渠道贴出可利用细节；
- 想提前试上面这些改动、参与验证的，可通过联系邮箱沟通。
